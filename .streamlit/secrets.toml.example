# Copy to `secrets.toml` in this folder (do not commit real keys).
# Streamlit loads `.streamlit/secrets.toml` automatically.
#
# Prefer project-root `.env` (see `../.env.example`) — loaded by the app via python-dotenv.

OPENROUTER_API_KEY = "sk-or-v1-..."
